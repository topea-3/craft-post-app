# はがき宛名面 プレビュー画面モック（v1）

- **画面 ID**: `PRT003`
- **画面名**: 印刷プレビュー・レイアウト調整
- **設計書**: [postcard-address-print-v1-design.md](../../design/postcard-address-print-v1-design.md)

---

## 1. 画面レイアウト

```
+------------------------------------------------------------------+
| [← 戻る]  印刷プレビュー    [種別: 年賀状 ▼]  [基準に戻す] [印刷] |
+------------------------------------------------------------------+
|  << 1 / 3 >>  （一括印刷時。各ページ=宛名+紐づき差出人）           |
+----------------------------+-------------------------------------+
|                            |  レイヤー                            |
|   [HTMLプレビューキャンバス]  |  ☑ 宛名 郵便番号                    |
|   100×148mm 縦横比固定      |  ☑ 宛名 住所1                       |
|   ドラッグで選択レイヤー移動  |  ☑ 宛名 氏名                        |
|   （layoutSpec座標の正）     |  ☐ 差出人 郵便番号  （今回のみ非表示）  |
|                            |  ...                                |
|                            |  [調整を保存] （永続化・クランプ済み）  |
+----------------------------+-------------------------------------+
```

**種別セレクト**: 入場時に選択（FR-09）。年賀状 / 喪中はがき（`nenga` / `mochu`）。**初回入場デフォルト: 年賀状（`nenga`）**。同一セッション再入場は `sessionStorage.printPostcardType` から復元（`printJobDraft` とは別キー）。切替時は未保存 offset があれば確認ダイアログ。

---

## 2. 操作

| 操作 | 挙動 |
|------|------|
| 種別セレクト | テンプレート定数切替。未保存 offset は確認。選択値を `printPostcardType` に保存 |
| キャンバス上ドラッグ | 選択レイヤーの offset 変更。**pt 揃え後**の結果座標（基準原点_pt + offset_pt）が印刷可能範囲内に収まるようクランプ。オフセットはジョブ全体で 1 組 |
| 基準に戻す | 選択レイヤーまたは全体の offset を 0 に |
| レイヤー ON/OFF | セッションのみ・ページごと可。PDF 生成時に反映 |
| 調整を保存 | `print_layout_preferences` に書き込み（クランプ済み） |
| 印刷 | 全件再スナップショット → **新規 `printJobId` 発行** → PDF 生成 → 送付記録 → **INSERT 成功後**に `printJobId` 破棄 → PDF 保存 / OS 印刷（任意） |
| 再試行 | INSERT 失敗時のみ表示。PDF 再生成なし・同一 `printJobId` で `create_postcard_sends_batch` のみ。印刷/再試行は実行中 disabled |
| ページ送り | 一括時。宛名ごとに紐づき差出人が異なる |

---

## 3. エラー表示

- 再スナップショット失敗（archived 等）: 「印刷対象に無効な宛名が含まれています」— all-or-nothing で停止
- PDF 生成失敗: 「PDF の生成に失敗しました」（送付記録なし。UUID 未使用扱い）
- 送付記録失敗: 「PDF は生成済みですが、送付記録に失敗しました。[再試行]」（UUID 保持・PDF 再生成なし）
- 再印刷（位置微調整後の [印刷]）: 新 `printJobId` → 新しい送付行が作成される（意図した動作）

---

## 4. プレビューと PDF の関係

- 調整 UI = HTML キャンバス（操作しやすさ優先）
- 印字結果 = スパイク結果に応じ react-pdf または html2canvas
- 座標は共通 `layoutSpec` 経由（mm→pt・Y 軸変換は `usePrintJob`）。実寸 PDF を正とし、±1mm 以内を手動確認する
- html2canvas 一括時は 1 ページずつキャプチャして同一 PDF に append（同時保持 1 枚）
